{% set quantity = serviceCart().totalQuantities %}
{% import 'layout/_lazyFocusImager' as macroLazyFocusImager %}
<header class="header">
    <div class="container d-none d-lg-block">
        <div class="row">
            <div class="topbar section hidden-sm col-lg-12 col-md-12 col-sm-12">
                {% set bannerTop = getBannerByPositionCode('BANNER_VIEW_TOP') %}
                {% if(bannerTop is not empty) %}
                    <div class="topbar section hidden-sm col-lg-12 col-md-12 col-sm-12">
                        {% for bnTop in bannerTop %}
                            {% set image = bnTop.imageSrc %}
                            {% set options = {
                                alt: bnTop.name,
                                lazy: true,
                                openDiv: false,
                            }%}
                            <a href="{{ bnTop.link }}" class="section block a-center">
                                {{ macroLazyFocusImager.LazyFocusImager(image,options) }}
                            </a>
                        {% endfor %}
                    </div>
                {% endif %}
                <a class="section block a-center" href="" title="Đồ trung thu"><img class="img-fluid flex" src="/tpl/V0510/img/banner-01.jpg" alt="Đồ trung thu"></a>
            </div>
        </div>
    </div>
    <div class="section topservice">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 d-lg-none col-md-12 col-sm-12">
                    <div class="wp left_wp">
                        <div class="icon_planes a-block a-center">
							<span class="itemdes1">

								<a class="text-white" href="https://www.beemart.vn/trung-thu" title="GIẢM GIÁ TƯNG BỪNG - MỪNG TẾT TRUNG THU | Giảm tới 40% các mặt hàng >> Xem ngay">GIẢM GIÁ TƯNG BỪNG - MỪNG TẾT TRUNG THU | Giảm tới 40% các mặt hàng &gt;&gt; Xem ngay</a>
							</span>
                        </div>
                    </div>
                </div>
                <div id="call-of-action" class="col-lg-8 col-md-8 col-sm-8 d-none d-lg-block">
                    <div class="left_wp">
                        <div class="d-inline-block icon_plane dropdown"><img src="/tpl/V0510/img/icon_plane2.png"> Giao hàng toàn quốc
                            <span class="head-desc itemdes1 dropdown-content">
                Beemart hỗ trợ giao hàng tới tận tay khách hàng trên toàn quốc với chính sách vận chuyển linh hoạt và phù hợp từng địa điểm.<br>
                <a class="aheadmore" href="#">Xem thêm:Chính sách giao hàng</a>
            </span>
                        </div>
                        <div class="d-inline-block icon_change dropdown"><img src="/tpl/V0510/img/icon_change2.png"> Trả hàng trong 7 ngày
                            <span class="head-desc itemdes1 dropdown-content">
                Beemart nhận đổi trả hàng với các thiết bị dụng cụ làm bánh có sử dụng điện trong vòng 7 ngày.<br>
                <a class="aheadmore" href="">Xem thêm: Chính sách đổi trả hàng</a>
            </span>
                        </div>
                        <div class="d-inline-block icon_coin dropdown"><img src="/tpl/V0510/img/icon_coin2.png"> Chính sách tích điểm
                            <span class="head-desc itemdes1 dropdown-content">
                Khách hàng được tích điểm trên mỗi đơn hàng và được sử dụng để giảm giá cho đơn hàng tiếp theo.<br>
                <a class="aheadmore" href="">Xem thêm: Chính sách khách hàng</a>
                        </span>
                        </div>
                    </div>
                </div>
                <div class="col-lg-4 col-md-4 col-sm-4 d-none d-lg-block">
                    <div class="wp right_wp">
                        {% if(hasIdentity() != null) %}
                        <li><a href="/profile" rel="nofollow"><i class="fa fa-user"></i> Xin chào, {{ getUser().name? getUser().name: getUser().fullName }}</a> /&nbsp;</li>
                        <li><a href="/user/signout" rel="nofollow">Đăng xuất</a></li>
                        {% else %}
                        <div class="login_register">
                            <a href="/user/signin" class="icon_user text-white">Đăng nhập</a> / <a class="text-white" href="/user/signup">Đăng ký</a></div>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
    <div class="mid-header">
        <div class="container">
            <div class="row header_fixnav_mobile content_header px-lg-0">
                <div class="col-lg-3 col-md-3 col-sm-3 col-3">
                    <div class="logo">
                        <a href="/" class="logo-wrapper">
                            <img class="d-none d-lg-block" src="/tpl/V0510/img/logo.png" alt="logo beemart.vn">
                            <img class="d-lg-none logo-height" src="/tpl/V0510/img/logo_small.png" alt="logo beemart.vn">
                        </a>
                    </div>
                </div>

                <div class="col-9 col-lg-9 col-md-9 colsm_custome float-left">
                    <div class="menu-bar menu-bar-h nav-mobile-button d-lg-none pt-3 pt-lg-4">
                        <a href="javascript:;">
                            <img src="/tpl/V0510/img/i_menubar.png" alt="icon menu">
                        </a>
                    </div>
                    <div class="header-right dropdown">
                        <div class="top-cart-contain f-right">
                            <div class="mini-cart text-xs-center">
                                <div class="heading-cart cart_header d-none d-lg-block">
                                    <a class="img_hover_cart" href="/cart" title="Giỏ hàng">
                                        <span class="countCart count_item_pr" id="cart-total">{{ quantity }}</span>
                                        <div class="icon_hotline">
                                            <img src="//bizweb.dktcdn.net/100/004/714/themes/783993/assets/smart-carty.png?1600054006104" alt="giỏ hàng">
                                        </div>
                                    </a>
                                </div>
                                <div class="heading-cart cart_header d-lg-none">
                                    <a class="img_hover_cart" href="/cart" title="Giỏ hàng">
                                        <span class="countCart count_item_pr" id="cart-total">{{ quantity }}</span>
                                        <div class="icon_hotline">
                                            <img class="d-none d-lg-block" src='/tpl/V0510/img/bag_cart.png' alt="giỏ hàng">
                                            <p class="d-none d-lg-block mt-3">Giỏ hàng</p>
                                            <img class="d-lg-none" src="/tpl/V0510/img/bag_index.png">
                                        </div>
                                    </a>
                                </div>
                                <div id="cart-top" class="top-cart-content dropdown-content-cart">
                                    {% set totalCartMoney = serviceCart().totalMoney %}
                                    {% set products= serviceCart().getProducts %}
                                    {% set prodIds = [] %}
                                    {% set orderValuePrices = 0 %}
                                    {% if(products is not empty) %}
                                    {% set productShipFee = [] %}
                                    {% set countPs = 0 %}
                                    {% endif %}
                                    {% if(products) %}
                                    <ul id="cart-sidebar" class="mini-products-list count_li">
                                        <ul class="list-item-cart">
                                            {% for p in products %}
                                            {% set priceCurrent = p.price * p.quantity %}
                                            {% set orderValuePrices = priceCurrent + (p.price * p.quantity) %}
                                            {% set image = p.thumbnailUri %}
                                            {% set options = {
                                                alt: p.name,
                                                lazy: true,
                                                openDiv: false,
                                            }%}
                                            <li class="cart-item item  productid-{{ p.id }}" price="{{ p.price }}" discount="{{ p.moneyDiscount }}">
                                                <div class="border_list">
                                                    <a class="product-image" href="{{ p.viewLink }}" title="{{ p.name | raw }}">
                                                        {{ macroLazyFocusImager.LazyFocusImager(image,options) }}
                                                    </a>
                                                    <div class="detail-item">
                                                        <div class="product-details">
                                                            <p class="product-name">
                                                                <a class="text2line" href="{{ p.viewLink }}" title="{{ p.name | raw }}">{{ p.name }}</a>
                                                            </p>
                                                        </div>
                                                        <div style="width: 9%" class="a-center d-none d-md-block">
                                                            <a class="fa fa-trash removeCart remove__top" href="javascript: void(0)" rel="{{ p.id }}"></a>
                                                        </div>
                                                        <div class="product-details-bottom cart-item-price-quantity">
                                                            {% if(p.moneyDiscount) %}
                                                                {% set mnDiscount = ( p.moneyDiscount//p.quantity) %}
                                                                {% if (countPs == 1) %}
                                                                    {% set mnDiscount = serviceCart().moneyDiscount//p.quantity %}
                                                                {% endif %}
                                                                {% set ttMoney = p.price - mnDiscount %}
                                                                <span class="price current-price">{{ ttMoney | number_format(0) }} ₫</span><br>
                                                                <span class="price-old">{{ p.price | number_format(0) }} ₫</span>
                                                                <span class="price-old">KM: -{{ mnDiscount | number_format(0) }} ₫</span>
                                                            {% else %}
                                                                <span class="price current-price">{{ p.price | number_format(0) }}₫</span>
                                                            {% endif %}
                                                            <div class="quantity-select qty_drop_cart">
                                                                <button class="btn_reduced reduced items-count btn-minus minus reduced_pop items-count btn-minus" type="button">-</button>
                                                                <input type="text" class="input-text number-sidebar input_pop input_pop updateCart quantity" data-qty="{{ p.quantity }}" min="1" max="{{ p.inventory.calcAvailable }}" value="{{ p.quantity }}"
                                                                       maxlength="10" psId={{ p.id }} >
                                                                <button class="btn_increase increase items-count btn-plus plus increase_pop items-count btn-plus" type="button">+</button>

                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </li>
                                            {% endfor %}
                                        </ul>
                                        <div class="pd">
                                            <div class="top-subtotal">Tổng tiền:
                                                <span class="price price_big totals_price">{{ totalCartMoney | number_format(0) }}₫</span>
                                            </div>
                                        </div>
                                        <div class="pd right_ct">
                                            <a href="/cart" class="btn btn-primary">
                                                <span>Vào trang giỏ hàng</span>
                                            </a>
                                        </div>
                                    </ul>
                                    {% else %}
                                    <ul id="cart-sidebar" class="mini-products-list count_li">
                                        <div class="no-item">
                                            <p>Không có sản phẩm nào.</p>
                                        </div>
                                    </ul>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>


                    <div class="col-search-engine">
                        <div class="header_search">
                            <form id="searchauto" class="input-group search-bar" action="/search" method="get" role="search">
                                <input id="searchtext" type="text" name="query" value="" placeholder="Tìm kiếm sản phẩm " class="input-group-field st-default-search-input search-text" autocomplete="off" required="">
                                <span class="input-group-btn d-none d-lg-block">
			<button type="submit" id="searchsubmit" class="btn icon-fallback-text d-none d-lg-block">
                <a id="show_search_smart" href="javascript:void(0)">Tìm kiếm</a>
			</button>
		</span>
                                <span class="input-group-btn d-lg-none">
			<button type="submit" id="searchsubmit_mobile" class="btn icon-fallback-text">
                <a id="show_search_smart_mobile" href="javascript:void(0)"><i class="fa fa-search"></i></a>
			</button>
		</span>
                            </form>
                        </div>
                    </div>

                    <div class="support d-none d-lg-block">
                        <a href="tel:1900XXXXXX" title="Gọi ngay">1900 XXX XXX</a>
                        <p>8h - 20h T2 - CN</p>
                    </div>
                    <div class="notify dropdown d-none d-lg-block">
                        {% set lastestNews = getLastestNews({'limit':6}) %}
                        <span class="img"><img src="//bizweb.dktcdn.net/100/004/714/themes/783993/assets/notify.png?1600054006104" data-lazyload="//bizweb.dktcdn.net/100/004/714/themes/783993/assets/notify.png?1600054006104" alt="notify"><i> {{ lastestNews | length}}</i></span>
                        <div class="notify_body dropdown-content">
                            <ul class="mt-0">
                                {% if(lastestNews is not empty) %}
                                    {% for n in lastestNews %}
                                        {% set image = n.imageUri %}
                                        {% set options = {
                                            alt: n.title|e('html'),
                                            lazy: true,
                                            openDiv: false,
                                        }%}
                                        <li>
                                            <a href="{{ n.viewLink }}" title="{{ n.title|e('html') }}">
                                            </a><div class="thumb"><a href="{{ n.viewLink }}" title="{{ n.title|e('html') }}">
                                                    {{ macroLazyFocusImager.LazyFocusImager(image,options) }}
                                                </a>
                                            </div>
                                            <div class="content_r">
                                                <h3>
                                                    <span>{{ n.title|e('html') }}<a href="{{ n.viewLink }}" title="{{ n.title|e('html') }}">...Chi tiết</a></span>
                                                </h3>
                                                <p class="date">{{ n.createdDateTime|date("d/m/Y") }}</p>
                                            </div>

                                        </li>
                                    {% endfor %}
                                {% endif %}
                                <li class="last_li">
                                    <a class="block last" href="/news" title="Xem tất cả thông báo">Xem tất cả thông báo</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="wrap_main main_menu_fixscroll d-none d-lg-block nav_to_fixed">
        <div class="container">
            <div class="row row-noGutter-2">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 relative">
                    <div class="bg-header-nav">
                        <div class="row ">
                            {% if(menuIsExisted({'type': 'header' })) %}
                                {% include 'layout/menucustom' %}
                            {% else %}
                                {% include 'layout/menucache' %}
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</header>

<div class="menu_mobile sidenav max_991 d-lg-none" id="mySidenav">
    <div class="acction">
        <div class="log">
            <div class="linkacc">
                <span><img src="//bizweb.dktcdn.net/100/004/714/themes/779684/assets/account_mobile.png?1596452952538" alt="as"></span>
                {% if(hasIdentity() != null) %}
                    <a href="/profile" rel="nofollow"><i class="fa fa-user"></i> Tài Khoản </a>
                    <a href="/user/signout" rel="nofollow"> / Đăng xuất</a></li>
                {% else %}
                    <a href="/user/signin">Đăng nhập&nbsp;/&nbsp;</a>
                    <a href="/user/signout">Đăng ký</a>
                {% endif %}
            </div>
        </div>
    </div>
    <div class="action_menu">
        <a href="/"><i class="fa fa-home"></i>Trang chủ</a>
        <a class="danhmucsp" href="javascript:;"><i class="fa fa-th"></i>Tất cả danh mục</a>
        <a href="/news"><span class="count_noti">{{ lastestNews | length}}</span><span class="fa"><img src="//bizweb.dktcdn.net/100/004/714/themes/779684/assets/notify.png?1596452952538" alt="al"></span>Thông báo</a>
    </div>
    <div class="action_link">
        <div>
            <p>
                Thông tin
            </p>
            <a href="/product">Hàng mới về</a>
            {% set newsCategory = getNewsCategories() %}
            {% if(newsCategory is not empty) %}
            {% for n in newsCategoryshow_search_smart %}
            <a href="{{ n.viewLink }}">{{ n.name }}</a>
            {% endfor %}
            {% endif %}
            {#
            <a href="/chinh-sach-doi-hang-n33532.html">Chính sách khách hàng</a>
            <a href="/chinh-sach-van-chuyen-n19506.html">Chính sách vận chuyển</a>
            <a href="/cac-phuong-thuc-thanh-toan-khi-mua-sam-tai-ovmart-n33533.html">Phương thức thanh toán</a>
        #}
        </div>
    </div>
    <div class="action_link">
        <div>
            <p>
                Hỗ trợ
            </p>
            <a href="/">Hotline: 1900 000 000 (8h-20h)</a>
            <a href="/">Email: support@abc.vn</a>
        </div>
    </div>



</div>
<div class="menu_cate_main d-lg-none">
    <div class="wrap">
        <div class="content_menu">
            {% if(menuIsExisted({'type': 'header' })) %}
                {% include 'layout/menucustome_mobile' %}
            {% else %}
                {% include 'layout/menumobile' %}
            {% endif %}
        </div>
    </div>
</div>